import { FormEvent, useEffect, useState } from "react";
import { Link, createFileRoute, useNavigate } from "@tanstack/react-router";
import { ArrowRight, Eye, EyeOff, KeyRound } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { QuizStageMark } from "@/components/quizstage-shell";
import { lovable } from "@/integrations/lovable/index";
import { supabase } from "@/integrations/supabase/client";

export const Route = createFileRoute("/auth")({
  validateSearch: (search: Record<string, unknown>) => ({ mode: search.mode === "register" ? "register" : "login" }),
  head: () => ({ meta: [{ title: "Host sign in — QuizStage" }, { name: "description", content: "Sign in to build and host your QuizStage live rooms." }, { property: "og:title", content: "Host sign in — QuizStage" }, { property: "og:description", content: "Build and host presentation-led live quizzes." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }),
  component: AuthPage,
});

function AuthPage() {
  const navigate = useNavigate();
  const { mode } = Route.useSearch();
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [busy, setBusy] = useState(false);
  const isRegister = mode === "register";
  useEffect(() => { void supabase.auth.getSession().then(({ data }) => { if (data.session) void navigate({ to: "/dashboard" }); }); }, [navigate]);
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { const result = isRegister ? await supabase.auth.signUp({ email, password }) : await supabase.auth.signInWithPassword({ email, password }); if (result.error) throw result.error; toast.success(isRegister ? "Account created." : "Welcome back."); void navigate({ to: "/dashboard" }); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not complete that request."); } finally { setBusy(false); } }
  async function google() { setBusy(true); const result = await lovable.auth.signInWithOAuth("google", { redirect_uri: window.location.origin }); if (result.error) { toast.error(result.error.message); setBusy(false); } }
  return <main className="grid min-h-screen lg:grid-cols-[.8fr_1.2fr]"><section className="hidden bg-ink p-10 text-paper lg:flex lg:flex-col lg:justify-between"><QuizStageMark /><div><p className="max-w-sm font-display text-6xl font-black uppercase leading-[0.9]">The room is waiting.</p><div className="mt-10 h-1 w-20 bg-brand" /><p className="mt-5 max-w-sm text-sm leading-6 text-paper/60">Create a deck, tune the questions, and take the controls when it is time to play.</p></div><p className="font-mono text-xs uppercase tracking-[0.16em] text-paper/40">QS / HOST CONSOLE</p></section><section className="flex items-center justify-center px-5 py-12"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><QuizStageMark /></div><p className="text-xs font-bold uppercase tracking-[0.18em] text-brand">{isRegister ? "Start hosting" : "Welcome back"}</p><h1 className="mt-4 font-display text-5xl font-black uppercase leading-none">{isRegister ? "Make some noise." : "Take the stage."}</h1><p className="mt-5 text-muted-foreground">{isRegister ? "Your first live room is a few clicks away." : "Sign in to manage your decks and rooms."}</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.12em]">Email</span><Input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required placeholder="you@company.com" /></label><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.12em]">Password</span><div className="relative"><Input className="pr-11" type={showPassword ? "text" : "password"} value={password} onChange={(event) => setPassword(event.target.value)} required minLength={6} placeholder="At least 6 characters" /><button type="button" aria-label={showPassword ? "Hide password" : "Show password"} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground" onClick={() => setShowPassword((value) => !value)}>{showPassword ? <EyeOff /> : <Eye />}</button></div></label><Button className="w-full" size="lg" disabled={busy}>{isRegister ? "Create host account" : "Sign in"}<ArrowRight /></Button></form><div className="my-7 flex items-center gap-3 text-xs uppercase tracking-[0.14em] text-muted-foreground"><span className="h-px flex-1 bg-border" />or<span className="h-px flex-1 bg-border" /></div><Button type="button" variant="outline" className="w-full" size="lg" onClick={() => void google()} disabled={busy}><span className="font-bold">G</span> Continue with Google</Button><p className="mt-8 text-center text-sm text-muted-foreground">{isRegister ? "Already hosting?" : "New to QuizStage?"}{" "}<Link className="font-semibold text-brand hover:underline" to="/auth" search={{ mode: isRegister ? "login" : "register" }}>{isRegister ? "Sign in" : "Create an account"}</Link></p><p className="mt-10 flex items-center justify-center gap-2 text-center text-xs text-muted-foreground"><KeyRound className="h-3.5 w-3.5" /> Your rooms and decks stay tied to your account.</p></div></section></main>;
}